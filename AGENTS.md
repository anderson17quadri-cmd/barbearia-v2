# Expo v56 + Expo Router

- Read https://docs.expo.dev/versions/v56.0.0/ before writing code
- Run `npx tsc --noEmit` for type checking
- Run `npx expo export --platform web` for build verification
- Use expo-router (file-based routing in src/app/)
- Theme: dark (#080808) + gold (#C9A84C)
